import React from 'react';

const Main: React.FC = () => {

    return (
        <div
            className={`w-full h-screen flex flex-col 
            items-center justify-start bg-first `}
        >
            Main
        </div>
    );
};

export default Main;
